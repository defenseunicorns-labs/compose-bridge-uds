apiVersion: v1
kind: Service
metadata:
    name: redis
    namespace: hello-world
    labels:
        app.kubernetes.io/name: redis
        app.kubernetes.io/part-of: hello-world
spec:
    selector:
        app.kubernetes.io/name: redis
    ports:
        - name: http
          port: 6379
          targetPort: 6379
          protocol: TCP
